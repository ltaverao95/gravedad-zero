export namespace CoreConstants{
    export enum EnumUserPermission {
        ADMIN = "admin",
        EDIT = "edit",
        READ = "read"
    }

    export enum EnumPostType {
        NEW = 0,
        POST = 1
    }
}